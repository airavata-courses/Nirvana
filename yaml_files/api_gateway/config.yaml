apiVersion: v1
kind: Service
metadata:
  name: api-gateway
spec:
 type: LoadBalancer
 ports:
  - targetPort: 5005
    port: 5005
    nodePort: 30005
 selector:
  app: python
--
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
spec:
  selector:
    matchLabels:
      app: python
  replicas: 3
  template:
    metadata:
      labels:
        app: python
    spec:
      containers:
      - name: api-gateway-microservice
        imagePullPolicy: Always
        image: juhideshkar30/api_gateway
        ports:
        - containerPort: 5005