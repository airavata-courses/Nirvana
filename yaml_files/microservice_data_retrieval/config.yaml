apiVersion: v1
kind: Service
metadata:
  name: data-retrieval-microservice
spec:
 type: LoadBalancer
 ports:
  - targetPort: 5000
    port: 5000
    nodePort: 30002
 selector:
  app: python
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-retrieval-microservice
spec:
  selector:
    matchLabels:
      app: python
  replicas: 3
  template:
    metadata:
      labels:
        app: python
    spec:
      containers:
      - name: data-retrieval-microservice
        imagePullPolicy: Always
        image: juhideshkar30/data_retrieval
        ports:
        - containerPort: 5000