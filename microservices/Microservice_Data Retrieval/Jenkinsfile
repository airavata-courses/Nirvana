pipeline {
    environment {
    registry = "juhideshkar30/data_retrieval"
    registryCredential = 'dockerhub'
    dockerImage = ''
   }
    agent any
    stages {
        stage('Building image') {
            docker.build()
        }
    }
}
