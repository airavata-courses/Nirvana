pipeline {
    agent
    {
        dockerfile true
    }
    stages {
        stage('testing') {
            steps {                 
                sh '''
                   git checkout Microservice_DataRetrieval
                   cd "microservices/Microservice_Data Retrieval/"
                   docker build -t image_name .
                '''    
             }
        }
    }
}
